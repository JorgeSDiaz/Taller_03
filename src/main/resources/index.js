// Definimos una variable y le asignamos un valor
var mensaje = "Hola, mundo!";

// Imprimimos el mensaje en la consola del navegador
console.log(mensaje);

// Definimos una función que toma dos números como argumentos y los suma
function sumar(a, b) {
    return a + b;
}

// Llamamos a la función y mostramos el resultado en la consola
var resultado = sumar(5, 7);
console.log(resultado);
